#
# Write a program that accepts a string as input, capitalizes the first letter of each
# word in the string, and then returns the result string. Examples: "hi"=> returns
#"Hi"
def capitalize(input):
    formattedInput = input.strip()
    print(formattedInput.title())
    
capitalize('     hey there fdnf sjdb ')